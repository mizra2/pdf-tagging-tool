import { defineStyleConfig } from "@chakra-ui/react";

const pdfGridStyleConfig = defineStyleConfig({
    baseStyle: {
        width: "100%",
        pt: "170px",
        flexShrink: "0",
        justifyContent: "center"
    }
});

export default pdfGridStyleConfig;
import { ModalHeader } from "@chakra-ui/react";

// Upload Header Modal
const UploadModalHeader = () => {
    return (<ModalHeader
        fontSize="26px"
        fontFamily="'Outfit', sans-serif"
        lineHeight="normal"
        fontStyle="normal"
        color="#222427"
        fontWeight={30}
        ml={3}
        mt={5}>
        Upload and attach files
    </ModalHeader>)
}
export default UploadModalHeader;